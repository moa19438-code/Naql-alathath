name: customer_app
description: Customer App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # البكج المشترك (UI)
  app_ui:
    path: ../packages/app_ui

  # اختياري (مفيد لاحقًا)
  cupertino_icons: ^1.0.8

flutter:
  uses-material-design: true
